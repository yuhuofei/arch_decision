# 规则库评审报告 — arch_decision

> **状态：已全部修复（P0 + P1 + P2）。** 本文件保留为修复前的审计记录。
> 修复内容见 `CHANGELOG.md` 的 `[v1.1] — 2026-09-19`；校验结果：`python3 scripts/validate_rules.py` → 0 错误 0 警告。
> 修复后新增文件：`.sdd/LAYOUT.md`、`.sdd/CONVENTIONS.md`、`.sdd/TRACEABILITY.md`、`.sdd/knowledge/{ai-llm,data,caching}.md`、
> `.sdd/decision-trees/ai-llm.md`、`.sdd/workflows/small-change.md`、`.sdd/schema/decision.schema.json`、
> `README.md`、`CHANGELOG.md`、`.gitignore`、`sources/`、`scripts/`、`specs/001-project/plan.md`、`specs/001-project/adr/×3`。

> 评审对象：本仓库全部受版本控制文件 + 3 份源文档（现已归档至 `sources/v1.0/`）
> 评审日期：2026-09-19
> 用途定位：**Agent 在 SDD 模式下做项目框架/技术栈选择时的决策规则库**
> 评审方法：逐文件通读 → 交叉核对源文档（`res.md` / `AI Architecture Decision Matrix.md` / `AI Coding SDD …知识库.md`）→ 验证每条结论的 `文件:行` 证据

---

## 0. 总体结论

规则库内容质量高、体系完整（约束模型 / 决策状态 / 复杂度预算 / 决策循环 / 领域矩阵均已落地），
**核心问题不在"缺内容"，而在"三个来源给出了三到四套互相冲突的产物约定，仓库只落地了其中一套，却没有声明取舍"**。

因此最优先的修改不是补新知识，而是**消除自相矛盾 + 声明唯一约定**，否则 Agent 会因读到不同来源而生成不同结构、对同一项目算出不同架构结论。

| 级别 | 数量 | 性质 | 是否影响 Agent 正确决策 |
| --- | ---: | --- | --- |
| P0 | 4 | 自相矛盾 / 口径不一 | **是，必须改** |
| P1 | 9 | 结构性与内容缺口 | 是，强烈建议 |
| P2 | 5 | 工程化增强 | 否，可选 |

---

## 1. P0 — 自相矛盾，必须修改

### P0-1 四套目录约定并存，未声明采纳哪一套（最高优先级）

源文档各自给出了**互不兼容**的产物目录约定：

| 来源 | 约定 |
| --- | --- |
| `res.md` §92（L2099-2112） | `docs/specs/001-feature-name/{context, requirements, technology-selection, design, decisions, tasks, verification}.md` |
| `res.md` §93（L2113-2121） | 项目级 `docs/{architecture, technology-selection, engineering-rules}.md` + `docs/specs/` |
| `res.md` 结尾"关键建议" | `.sdd/{knowledge, decision-trees, templates, workflows}/` + `specs/001-project/` |
| `AI Architecture Decision Matrix.md` §45（L1996-2030） | `ai-architecture-kb/{README, principles, decision-matrix, decision-tree, scoring}.md` + `{languages, backend, frontend, database, infrastructure, architecture, security, testing, observability}/` + `templates/` + `examples/` |
| `知识库` §78（L2368-2384） | `specs/001-order-management/{spec, plan, tasks, research, data-model, api-contract, architecture}.md` + `adr/ADR-00x-*.md` |

仓库实际只落地了第 3 套（`.sdd/` + `specs/`）。

**残留证据**：`.sdd/templates/spec.md` 第 4 行仍在引用 `docs/architecture.md` + `docs/technology-selection.md` + `docs/engineering-rules.md`（§93 的残留），而仓库中并无 `docs/` 目录。

**影响**：Agent 读到 `spec.md` 模板会去 `docs/` 找文件；读到 Matrix §45 会以为应建 `ai-architecture-kb/`。

**修复**：新增 `.sdd/LAYOUT.md`（或 `.sdd/README.md` 增一节）显式声明：
1. 采纳约定 = `.sdd/` + `specs/<id>-<name>/`；
2. 其余 3 套标记 `superseded` 并给映射表（旧路径 → 新路径）；
3. 删除 `templates/spec.md:4` 的 `docs/` 引用。

### P0-2 `spec.md` 的节数与命名三处不一致

| 来源 | 内容 |
| --- | --- |
| `res.md` §117（L2758 起） | spec.md 应含 **23 项**，含 Architecture(8) / Technology Decisions(9) / Repository Structure(10) / Logging(16) / CI/CD(18) / Deployment(19) |
| `知识库` §79（L2390 起） | **14 节** 模板（无 Architecture / Technology Decisions / Repository Structure） |
| 仓库 `.sdd/templates/spec.md` | **14 节**（采纳知识库 §79） |

**过期/错误引用**：
- `.sdd/workflows/new-feature.md` L15：「不必全量 **23 项**」——模板已是 14 节，引用失效。
- `specs/001-project/verification.md` L6：「Acceptance Criteria（spec.md **§23**）」——模板中 Acceptance Criteria 是 **§10**。

**语义冲突**：`res.md` §117 把 Architecture / Technology Decisions 放进 spec.md；而 `CLAUDE.md` §7 Golden Rule 与 `.sdd/templates/spec.md` 明确「spec = WHAT/WHY，plan = HOW」。两处结论相反，未记录取舍理由。

**修复**：声明「以知识库 §79 的 14 节为准，架构内容下沉 `plan.md`」，并补一张 `res.md §117(23项) → 14节/plan.md` 映射表；修正上述两处引用。

### P0-3 架构复杂度预算计分口径不一致

`decision-protocol.md` §5（L110-122）只写「每引入一个基础设施组件 +1（Kafka/ES/Vector +2；K8s/Microservices +3）」，未定义 Docker、对象存储、部署平台、语言框架是否计分，也未定义 **pgvector 作为 PostgreSQL 扩展时如何归属**。

结果三个示例算出三种口径：

| 文件:行 | 算式 | 是否含 Docker |
| --- | --- | --- |
| `.sdd/examples/saas.md` L59 | `PG(1)+S3(1)+Docker(1)=3` | 计 |
| `.sdd/examples/high-concurrency.md` L36 | `PG(1)+[Redis?]+[gRPC 不计]=1-2` | **不计** |
| `.sdd/examples/ai-saas.md` L41 | `PG(1)+pgvector(2,计入Vector)+S3(1)=4-5` | 不计 |

**影响严重**：复杂度预算是"是否超限 → 是否重评 → 是否触发人工确认"的判据。口径不一会让同类项目得出相反结论。

**修复**：在 `decision-protocol.md` §5 补一张**明确的计分表**：

- 计入：独立部署的基础设施组件（DB / Cache / MQ / Search / Vector / 对象存储 / 调度器 / K8s 等）
- 不计入：语言、框架、ORM、Docker/Docker Compose（属打包方式）、CI/CD、gRPC（协议）
- pgvector：作为 PG 扩展时**不额外 +2**（同实例）；独立向量库才 +2

并回头统一三个示例的算式。

### P0-4 两套 `§编号` 体系混用

全仓同时使用两种 `§`：`res.md` 的 120 条编号（如 `§67` = Transaction）与本地文件节号（如 `decision-protocol §6`）。
例：`.sdd/knowledge/database.md` L51-54 用 `§67,§68,§69` 指 res.md；同一文件 L4 又用 `decision-protocol` 指本地。
更危险的是**同数字在两套体系中含义不同**：`res §67` = Transaction，而 `spec.md §10` = Acceptance Criteria。

**修复**：约定前缀 —— res.md 引用统一写 `res:§67`，本地引用写 `<文件名>§N`；全仓批量替换。

---

## 2. P1 — 结构与内容缺口，强烈建议修改

| # | 问题 | 证据 | 建议 |
| --- | --- | --- | --- |
| P1-1 | **缺 `knowledge/ai-llm.md`**：AI/LLM/RAG/Agent 决策线未落地 | Matrix §29(L1312)/§30(L1337)/§31(L1365) 仅被 `.sdd/examples/ai-saas.md` L33 一句话覆盖；知识库 §60(L1875) 未进 knowledge | 新增 AI 决策线：`model_provider / prompt_management / token_cost / retry / timeout / streaming / structured_output / evaluation / fallback / observability`；LLM Provider 抽象触发条件；RAG 检索架构；Agent 框架与 eval |
| P1-2 | **缺 `knowledge/data.md`**：数据工程/管道无知识文件 | `knowledge/architecture.md` L15 已列 "Data Application/ETL" 类型，`knowledge/testing.md` L19 已列 "Data Pipeline" 策略，但无对应决策知识 | 新增：调度（Airflow/Dagster/Prefect）、dbt、批 vs 流（Spark/Flink）、特征存储、数据质量、幂等与回填 |
| P1-3 | **Search / Caching 仅有残片** | Matrix §17(L840) / §23(L1084) 未落地；仓库仅 `infrastructure.md` L38-42（Search 4 行）、`api.md` L39-42（cache-aside 2 行） | 补 `knowledge/caching.md`（含 Matrix §23 触发条件）或把两处扩写为完整决策线 |
| P1-4 | **`specs/001-project/` 示例不完整** | 缺 `plan.md`（模板套件核心件）；`technology-selection.md` L103-105 的 ADR 索引指向 `adr-001-postgres.md`/`adr-002-fastapi.md`，但 `specs/001-project/` 下**无 `adr/` 目录** | 补 `plan.md` 实例 + `adr/` 目录与 2 个 ADR 样例（否则 Agent 无「照抄样例」） |
| P1-5 | **无根 `README.md`、无 `.gitignore`、版本化未落地** | 根目录仅 `AGENTS.md`/`CLAUDE.md`/`res.md`/2 份源文档/`specs/`；`CLAUDE.md` L59 与 `.sdd/README.md` L62 建议「知识库版本化 v1.0/v1.1」但无 `VERSION`/`CHANGELOG.md` | 加根 `README.md`（总览 + 阅读顺序）、`.gitignore`、`CHANGELOG.md` |
| P1-6 | **源文档未归档，与生成物混放** | `res.md`、`AI Architecture Decision Matrix.md`、`AI Coding SDD…知识库.md` 平铺根目录 | 移入 `sources/v1.0/` + `sources/README.md` 声明「只读来源，Agent 决策时不读取」 |
| P1-7 | **`.workbuddy/memory/` 已进入 git 跟踪** | `git ls-files` 含 `.workbuddy/memory/2026-09-18.md` | 从索引移除并写入 `.gitignore`（团队共享的规则库不应带个人工作日志） |
| P1-8 | **缺 `res.md 编号 → 本地文件` 反向索引** | 各文件头写「来源：res.md §x,§y」，全仓无映射表 | 加 `.sdd/TRACEABILITY.md`：120 条 → 落点文件，便于源规则升级时评估影响面 |
| P1-9 | **`AGENTS.md` 无「读取顺序」** | `CLAUDE.md` §2 有 8 步清单；`AGENTS.md`（44 行）只有规则摘要 | 给 `AGENTS.md` 补同样的 workflow 入口（Codex/Cursor 等读 `AGENTS.md`，否则拿不到流程） |

---

## 3. P2 — 工程化增强（可选）

| # | 建议 |
| --- | --- |
| P2-1 | 加 machine-checkable 校验：`schema/decision.schema.json` + 一个脚本，检查 `technology-selection` YAML 必填项与复杂度预算 |
| P2-2 | 技术选型评分模型（`Requirement Fit×40 + Maintainability×20 + Team Fit×15 + Operational Simplicity×15 + Ecosystem×10`）缺 **Cost** 维度；个人/小预算项目成本敏感，建议加一档或显式说明为何不加 |
| P2-3 | `BLOCKED` 状态只说「停止，请求澄清」，缺澄清问题模板；建议在 `decision-protocol.md` 补一节问题模板（与 QUESTION POLICY MUST/SHOULD/CAN ASK 对应） |
| P2-4 | 缺「轻量改动」档：`bugfix`/`refactor` 之外，小改动（改文案、调样式）也需跳过全流程的显式阈值 |
| P2-5 | 文件间规则冲突时的仲裁顺序未定义；建议补：`decision-protocol > decision-trees > knowledge > 默认值` |

---

## 4. 明确不建议改动（避免过度工程）

1. **不要合并 `knowledge/` 与 `decision-trees/`**：同一主题在两处出现是刻意设计（参考知识 vs 判断逻辑），便于按需读取。
2. **不要为对齐 Matrix §45 而新建 `ai-architecture-kb/`**：会导致 Agent 读到两套并行结构。应采纳 P0-1 的「声明 + 映射」方案。
3. **不要为消除 P2-2 而重写评分模型**：除非确有成本敏感项目，否则加维度反而稀释 `Requirement Fit` 的权重。
4. **`docs/` 目录**：若要保留 `res.md §93` 的项目级产物概念，应作为「项目实例的落地位置」而非「规则库位置」，并与 `specs/` 明确分工；**不要同时存在两套同义目录**。

---

# 二审（同日，复核一审修复后的产出）

一审修的是「内容自相矛盾」。二审复核时发现：**用于验证修复的工具本身有缺陷**，且一审遗漏了一条内容死胡同。

## R1（P1）追踪矩阵漏报 —— 最严重

`.sdd/TRACEABILITY.md` 的用途是「源文档升级时快速评估影响面」。生成它的正则
`(res\.md|Matrix|知识库) §(\d+)` 只认「来源名 + 紧随其后的**单个**条号」，因此：

| 写法 | 实际含义 | 旧解析识别到 | 位置（示例） |
| --- | --- | --- | --- |
| `Matrix §24,§25,§45` | §24、§25、§45 | 仅 §24 | `knowledge/deployment.md:3` |
| `res.md §50-§58` | §50…§58 | 仅 §50 | `knowledge/testing.md:3` |
| `知识库 §6.1` | 子条目（Python 框架） | 截断记成 §6 | `knowledge/backend.md` |
| `res.md §0,§91-§118,§116` | §0、§91…§118、§116 | 仅 §0 | `workflows/new-project.md:3` |

库内共 69 处压缩/区间写法、30+ 处子条目引用受影响。**实测覆盖率**：

| 来源 | 旧解析"被引用" | 真实情况（修复后） |
| --- | --- | --- |
| res.md | 77/121 = 64% | 132/132 = 100% |
| Matrix | 42/45 = 94% | 59/59 = 100% |
| 知识库 | 47/89 = 53% | 103/103 = 100% |

**为什么这是 P1 而非 P2**：该表会把 `Matrix §45`、`知识库 §79` 这类**实际被多处引用**的规则标成「未引用」。
若有人据此判断"这条没人用，可以删"，就会删掉仍有落点的规则。**假阴性比假阳性危险。**

**根因**：`gen_traceability.py` 与 `validate_rules.py` 各自维护一份同样的正则。
**修复**：抽出 `scripts/sdd_refs.py` 作为唯一实现（解析 + 扫描范围），两脚本共用。

## R2（P1）同源缺陷：越界引用漏检

`validate_rules.py` 用的是同一份正则，因此放过了压缩写法里的越界条号。修复解析后**立即抓到 1 处**：

- `.sdd/knowledge/architecture.md:3` → `Matrix §65-§69`，但 `Matrix` 最大条号为 **§45**。
  该区间实属 `知识库`（§65 微服务 / §66 EDA / §67 CQRS / §68 ES / §69 DDD），
  且同行的 `知识库 §65-§70` 已正确覆盖 → 属误并进 `Matrix` 组。已删除。

> 一审修过 3 处「来源错配」，但那是**人工语义核查**发现的；机器当时看不到这类错误。
> 修复解析后，机器能自己发现了。

## R3（P1）TS 后端选型死胡同

`.sdd/decision-trees/backend.md` §1 的语言判定含
`IF fullstack_web OR frontend OR node_backend → TypeScript # AUTO`，
但 §4「TypeScript 框架」只列 `Next.js / React + Vite / Vue + Vite` —— **三个都是前端框架**。
于是 `node_backend`（纯 TS API 服务）判定出语言后**无框架可依**。

同时 `AGENTS.md` 的默认技术矩阵声明来源为 `res.md §110`，却漏掉两行：

| res.md §110 原文 | 修复前 AGENTS.md |
| --- | --- |
| `Enterprise Backend → Spring Boot / NestJS` | **缺失** |
| `TS Backend → NestJS / Fastify / Hono` | **缺失** |

**修复**：`decision-trees/backend.md` §4 拆成「纯 TS 后端 → NestJS（默认）/ Fastify / Hono」与
「含前端的 fullstack → Next.js」两组，并写明"不要把 Next.js 当纯后端框架"；
`AGENTS.md` 矩阵按 `res.md §110` 补全 7 行。

## R4（P2）扫描范围不一致

`gen_traceability.py` 只扫 `.sdd/` 与 `specs/`，而 `validate_rules.py` 扫全仓。
于是 `AGENTS.md` / `CLAUDE.md` / `CHANGELOG.md` 里的引用（如 `res.md §102`、`知识库 §85`）对索引不可见。
→ 范围统一由 `sdd_refs.targets()` 提供。

## R5（P2）引用完整性：10 处「内容已有、未标出处」

R1 修复后暴露的真实清单（内容确实存在，只是没写来源条号），已逐条补标注。
剩余为 0 条 —— 但请注意：

> **引用率 ≠ 内容覆盖率。** 100% 只说明「每条源规则都至少被一处声明为来源」，
> 不说明已完整正确落地。二审只做了「有无出处」的机器校验与人工分诊，
> **未**逐条评审内容正确性。

## 二审未做的事（留待需要时）

1. **未做父子条目的引用闭包**：当前「引用 `知识库 §6`」不会推断「`§6.1` 已覆盖」，
   反之亦然。理由是：父条目被声明为来源，**不证明**其子条目被真正处理过——推断会削弱这个信号。
   代价是需在来源行显式列出子条目（本轮已补）。
2. **未评审 `res.md §79-§82`（Django/Go/NestJS/Spring Boot 目录模板）的内容深度**：
   已通过 `knowledge/backend.md` §7 指向 `res.md §78-§82` 建立落点，
   但仓库并未复制这些目录树。是否需要在 `.sdd/templates/plan.md` 里固化，取决于实际使用反馈。
